原始数据为horse-colic-data.txt和horse-colic-test.txt,
代码分为两部分,pre-deal-data.py对原始数据进行处理,得到所需数据,训练和测试所用数据分别输出到training.txt和test.txt中
 logres.py对数据使用L2正则化SGD,SGD和BGD三种方式进行处理和分类,并分别计算出了其损失函数,预测错误(成功)率,画出了ROC曲线并算出了AUC的值.
 经测试,两种SGD至少要700次迭代左右才能得到一个较低的错误率,BGD需要的迭代次数低一些,预测成功率也相对更好.但三者的错误率基本都在25%-30%之间.
  对L2正则化SGD,在迭代次数足够多时,λ的选取对错误率影响不是很大,但对AUC有一定影响,经粗略测试发现λ=20时AUC相对而言较大
